language: python

os:
    - linux
    - osx

python:
    - 3.6
    - 2.7

install:
    - pip install -r requirements.txt

script:
    - pytest

notifications:
  email:
    recipients:
      - one@example.com
      - other@example.com
    on_success: never
    on_failure: always
